-- ami_env_dev.cfg
-- REFERENCE ONLY. Not consumed by EXECUTE IMMEDIATE FROM.
-- Kept so reviewers can compare against prod ami_env_dev.cfg.

-- Common Schemas
!define CORP_SCH = AMICORP;
!define COMM_SCH = AMICOMM;
!define FRMWK_SCH = FRAMEWORK;
!define RPTS_SCH = AMIRPTS;
!define STAGE_SCH = AMISTAGE;
!define XREF_SCH = XREF;
!define CIAP_SCH = AMICIAP;
!define INT_SCH = AMIINT;

-- Environment specific. Demo mirrors DEV.
-- Note: DB_NAME deviates from prod (prod uses DEV_AMI_DEV_DB).
-- Demo uses AMI_DEMO_DB to keep demo isolation obvious.
!define DB_NAME=AMI_DEMO_DB;
!define RL_NAME=DEV_AMI_ADMIN_ROLE;
!define WH_NAME=NONPROD_AMI_ADMIN_WH;

-- Environment specific application roles
!define AMI_SEL_ROLE=DEV_AMI_SELECT_ROLE;
!define AMI_MAT_ROLE=DEV_AMI_LOAD_ROLE;
!define AMI_MLS_ROLE=DEV_AMI_MLS_ROLE;
!define AMI_PBI_ROLE=DEV_AMI_PBI_ROLE;
!define AMI_SUPPORT_ROLE=NONPROD_AMI_SUPPORT_ROLE;
